pipeline {
    agent {
        docker {
            image 'postman/newman:alpine'  // Utilise l'image Docker contenant Newman
           
        }
    }

    stages {
        stage('Run Tests avec Newman') {
            steps {
                script {
                  
                    sh 'newman run tp.json'
                }
            }
        }
    }
}
